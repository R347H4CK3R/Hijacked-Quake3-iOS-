# Standalone runtime inputs

This project must not require or redistribute Quake III Arena retail `pak0.pk3`.

The native iOS engine is built from the pinned GPL Quake3-iOS source. Standalone game logic must come from redistributable GPL-compatible Quake III/OpenArena game code. User-supplied Hijacked data remains outside the public repository and is injected only into the private build artifact.
